format_version: 6
default_step_lib_source: https://github.com/bitrise-io/bitrise-steplib.git

app:
  envs:
  # Define it (A_SECRET_PARAM) in .bitrise.secrets.yml
  # - ACCESS_KEY: $ACCESS_KEY
  # - USE_NAME: $USER_NAME

  - ACCESS_KEY: xD4vYRrzWyzZFVFCawGd
  - USER_NAME: akosbirmacher1
  
  - BITRISE_IPA_PATH: "/Users/akosbirmacher/Develop/Test_files/BullsEye/BullsEye_Dev_IPA/BullsEye.ipa"
  - XCUITESTS_RUNNER: "/Users/akosbirmacher/Develop/Test_files/BullsEye/BullsEyeUITests-Runner.app.zip"

workflows:
  test:
    before_run:
    # - audit-this-step
    steps:
    # - go-list:
    # - golint:
    # - errcheck:
    # - go-test:
    after_run:
    - test_xcuitests

  test_xcuitests:
    steps:
    - change-workdir:
        title: Switch working dir to test / _tmp dir
        description: |-
          To prevent step testing issues, like referencing relative
          files with just './some-file' in the step's code, which would
          work for testing the step from this directory directly
          but would break if the step is included in another `bitrise.yml`.
        run_if: true
        inputs:
        - path: ./_tmp
        - is_create_path: true
    - path::./:
        title: Step Test
        run_if: true
        inputs:
        - user_name: $USER_NAME
        - access_key: $ACCESS_KEY
        - test_type: XCUITests
        - xcuitests_ipa: $BITRISE_IPA_PATH
        - xcuitests_runner: $XCUITESTS_RUNNER

  # ----------------------------------------------------------------
  # --- workflows to Share this step into a Step Library
  audit-this-step:
    steps:
    - script:
        inputs:
        - content: |-
            #!/bin/bash
            set -ex
            stepman audit --step-yml ./step.yml
